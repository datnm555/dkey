# Xcode / XcodeGen
*.xcodeproj/
*.xcworkspace/
DerivedData/
build/
*.dmg

# macOS
.DS_Store

# Swift
.build/

# Local MCP config (may contain tokens) — do not commit
.mcp.json

# Compiled oracle binary (oracle.cpp is committed; binary is not)
scripts/openkey-oracle/oracle
scripts/openkey-oracle/convert-oracle
